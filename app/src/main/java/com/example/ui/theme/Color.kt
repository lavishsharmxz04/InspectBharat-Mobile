package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Official Indian Government & Enterprise Inspection Palette
val GovNavyPrimary = Color(0xFF0F2C59)       // Deep Official Navy
val GovNavyDark = Color(0xFF081B38)          // Darker Navy for Headers/Bars
val GovNavyLight = Color(0xFF1E427B)         // Lighter Navy for secondary accents
val GovNavyContainer = Color(0xFFE8EFFB)     // Light Navy Tint for Active Pills/Containers

val GovSaffronAccent = Color(0xFFD97706)     // Saffron / Amber Accent
val GovSaffronLight = Color(0xFFFEF3C7)      // Saffron Container / Warning tint

val GovIndiaGreen = Color(0xFF15803D)        // Compliant / Verified Green
val GovGreenLight = Color(0xFFDCFCE7)        // Green Badge background
val GovGreenDark = Color(0xFF166534)         // Dark Green for text

val GovRedDanger = Color(0xFFDC2626)         // Violation / Critical Red
val GovRedLight = Color(0xFFFEE2E2)          // Red Badge background

val GovAmberWarning = Color(0xFFB45309)      // Pending / Partial Compliance Amber
val GovAmberLight = Color(0xFFFEF3C7)

val GovBackground = Color(0xFFF8FAFC)        // Crisp light grayish administrative background
val GovSurface = Color(0xFFFFFFFF)           // Pure White Surface
val GovSurfaceVariant = Color(0xFFF1F5F9)    // Off-white administrative card background
val GovBorder = Color(0xFFCBD5E1)            // Standard administrative border
val GovBorderLight = Color(0xFFE2E8F0)

val GovTextPrimary = Color(0xFF0F172A)       // High-contrast slate dark text
val GovTextSecondary = Color(0xFF475569)     // Subtitle slate text
val GovTextMuted = Color(0xFF64748B)         // Placeholder / timestamp text

// Dark Theme Colors
val GovDarkBackground = Color(0xFF0B132B)
val GovDarkSurface = Color(0xFF1C2541)
val GovDarkSurfaceVariant = Color(0xFF243356)
val GovDarkTextPrimary = Color(0xFFF8FAFC)
val GovDarkTextSecondary = Color(0xFF94A3B8)
val GovDarkBorder = Color(0xFF334155)
